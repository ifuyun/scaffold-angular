import {HTTP_CODE} from './constants';

export enum API {
  login = '/login',
  logout = '/logout',
  userInfo = '/getUser'
}

export const API_WHITE_LIST = [];
export const RES_CODE_WHITE_LIST = [];
